@typeparam TArg
@typeparam TResult
@inherits UseEndpointWithArg<TArg, TResult>

@if (_query is not null)
{
    if (UseErrorBoundary && _query.IsError && _query.Error is not null)
    {
        throw _query.Error;
    }
    @ChildContent(_query)
}
