@typeparam TArg
@inherits UseEndpointWithArg<TArg, Unit>

@if (_query is not null)
{
    if (UseErrorBoundary && _query.IsError && _query.Error is not null)
    {
        throw _query.Error;
    }
    @ChildContent((Mutation<TArg>)_query)
}
