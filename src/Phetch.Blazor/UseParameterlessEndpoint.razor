@typeparam TResult
@inherits UseEndpointBase<Unit, TResult>

@if (CurrentQuery is not null)
{
    if (UseErrorBoundary && CurrentQuery.IsError && CurrentQuery.Error is not null)
    {
        throw CurrentQuery.Error;
    }
    @ChildContent((Query<TResult>)CurrentQuery)
}
