@typeparam TResult
@inherits UseEndpointBase<Unit, TResult>

@if (_query is not null)
{
    if (UseErrorBoundary && _query.IsError && _query.Error is not null)
    {
        throw _query.Error;
    }
    @ChildContent((Query<TResult>)_query)
}
